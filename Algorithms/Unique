#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int arr[] = {1, 1, 2, 2, 3, 3, 4, 5, 6, 7, 8, 6, 6, 6, 6, 8};
    int *k = unique(arr, arr + 16);

    for(int i=0;i<=*k;i++)
    {
        cout<<arr[i]<<" ";
    }
}
